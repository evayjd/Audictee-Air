[build]
builder = "NIXPACKS"
buildCommand = "cd backend && pip install -r requirements.txt && python -m spacy download fr_core_news_sm"

[deploy]
startCommand = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"